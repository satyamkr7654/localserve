import Link from "next/link";
import { CalendarClock } from "lucide-react";
import { BookingStatusBadge, Button, Card, SectionHeading, formatDateTime, formatMoney } from "@localserve/ui";
import { activeBooking } from "../../lib/preview-data";

export const metadata = { title: "Bookings" };
export default function BookingsPage() { const bookings = [activeBooking, { ...activeBooking, id: "0191265e-8c2f-7a1b-8d90-22ac9e464002", serviceName: "Electrical safety check", providerName: "Aman Verma", status: "CLOSED" as const, scheduledAt: "2026-07-28T10:30:00Z", etaMinutes: null }]; return <div className="space-y-6"><SectionHeading eyebrow="Your activity" title="Bookings" description="Authoritative status, payment and timeline history for your services."/><div className="grid gap-4">{bookings.map(booking => <Card key={booking.id} className="flex flex-wrap items-center justify-between gap-5 p-5"><div className="flex items-center gap-4"><span className="grid size-12 place-items-center rounded-2xl bg-primary-soft text-primary"><CalendarClock className="size-5"/></span><div><div className="flex flex-wrap items-center gap-2"><h2 className="font-black">{booking.serviceName}</h2><BookingStatusBadge status={booking.status}/></div><p className="mt-1 text-sm text-muted-foreground">{booking.providerName} · {formatDateTime(booking.scheduledAt)}</p></div></div><div className="flex items-center gap-4"><strong>{formatMoney(booking.amount)}</strong><Button asChild variant="secondary"><Link href={`/bookings/${booking.id}`}>View details</Link></Button></div></Card>)}</div></div>; }
